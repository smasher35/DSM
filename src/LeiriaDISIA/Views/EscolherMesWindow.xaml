<Window x:Class="LeiriaDISIA.Views.EscolherMesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Escolher Mês" Width="320" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource BrushBackground}" FontFamily="Segoe UI">
    <StackPanel Margin="20">
        <TextBlock Text="Escolher Mês" Style="{StaticResource SectionTitleStyle}" FontSize="16" Margin="0,0,0,4" />
        <TextBlock Text="Indique o ano e o mês do relatório a gerar." TextWrapping="Wrap" FontSize="11"
                   Foreground="{StaticResource BrushTextSecondary}" Margin="0,0,0,14" />

        <TextBlock Text="Ano" FontWeight="SemiBold" />
        <ComboBox x:Name="CmbAno" Margin="0,4,0,10" />

        <TextBlock Text="Mês" FontWeight="SemiBold" />
        <ComboBox x:Name="CmbMes" Margin="0,4,0,14" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancelar" Click="Cancelar_Click" Margin="0,0,10,0" />
            <Button Content="OK" Style="{StaticResource PrimaryButton}" Click="Ok_Click" IsDefault="True" />
        </StackPanel>
    </StackPanel>
</Window>
