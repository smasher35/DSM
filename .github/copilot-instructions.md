# Copilot Instructions

## Project Guidelines
- The user prefers to have clear file context and expects fixes to address UI sizing and data persistence issues. When fixing avatar systems, ensure proper resource handling (file streams, bitmap disposal) and use tracking flags for state changes.